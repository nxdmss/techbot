# 🚂 Пошаговая инструкция по деплою на Railway

## Шаг 1: Подготовка проекта

### 1.1. Убедитесь, что все файлы готовы

Проверьте наличие следующих файлов:
- ✅ `package.json` - с правильными скриптами
- ✅ `.gitignore` - исключает `node_modules` и `.env`
- ✅ `railway.json` - конфигурация Railway
- ✅ `.env.example` - пример переменных окружения
- ✅ `README.md` - документация

### 1.2. Получите необходимые данные

1. **Telegram Bot Token:**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Ваш Telegram ID:**
   - Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
   - Отправьте `/start`
   - Скопируйте ваш ID (число, например: `123456789`)

## Шаг 2: Загрузка проекта в GitHub

### 2.1. Инициализация Git (если еще не сделано)

```bash
cd /Users/nxdms/Desktop/techbot

# Убедитесь, что node_modules не в репозитории
git rm -r --cached node_modules 2>/dev/null || true

# Инициализируйте репозиторий
git init
git add .
git commit -m "Initial commit for Railway deployment"
```

### 2.2. Создайте репозиторий на GitHub

1. Откройте [GitHub](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий (например: `techbot`)
4. **НЕ** добавляйте README, .gitignore или лицензию
5. Нажмите "Create repository"

### 2.3. Загрузите код

```bash
# Добавьте remote
git remote add origin https://github.com/ваш-username/techbot.git

# Загрузите код
git branch -M main
git push -u origin main
```

## Шаг 3: Деплой на Railway

### 3.1. Создайте проект на Railway

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Авторизуйтесь через GitHub (если нужно)
5. Выберите ваш репозиторий `techbot`
6. Railway автоматически начнет деплой

### 3.2. Настройте переменные окружения

1. В проекте Railway откройте вкладку **Variables**
2. Нажмите **"+ New Variable"**
3. Добавьте следующие переменные:

   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   ADMIN_ID=ваш_telegram_id
   NODE_ENV=production
   ```

   **Важно:** Замените `ваш_токен_от_BotFather` и `ваш_telegram_id` на реальные значения!

4. Нажмите **"Add"** для каждой переменной

### 3.3. Настройте домен

1. Откройте вкладку **Settings**
2. Прокрутите до раздела **Networking**
3. Нажмите **"Generate Domain"** или **"Custom Domain"**
4. Скопируйте URL (например: `https://techbot-production.up.railway.app`)

### 3.4. Добавьте WEB_APP_URL

1. Вернитесь в **Variables**
2. Добавьте новую переменную:
   ```
   WEB_APP_URL=https://ваш-railway-url.railway.app
   ```
   Замените на ваш реальный URL из шага 3.3

3. Нажмите **"Add"**

### 3.5. Дождитесь деплоя

1. Откройте вкладку **Deployments**
2. Дождитесь завершения деплоя (зеленый статус)
3. Откройте вкладку **Logs** и проверьте:
   - ✅ `Токен бота загружен: ...`
   - ✅ `Бот успешно подключен: @username`
   - ✅ `Polling запущен успешно`
   - ✅ `🚀 Бот запущен!`

## Шаг 4: Проверка работы

### 4.1. Проверьте бота

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Должна появиться кнопка **"🛍️ Открыть магазин"**

### 4.2. Проверьте Web App

1. Нажмите на кнопку **"🛍️ Открыть магазин"**
2. Должно открыться веб-приложение с каталогом товаров
3. Попробуйте добавить товар в корзину

### 4.3. Проверьте заказы (для админа)

1. Если вы указали свой `ADMIN_ID`, отправьте боту `/orders`
2. Должны отображаться заказы от клиентов

## Шаг 5: Обновление проекта

После изменений в коде:

```bash
# Закоммитьте изменения
git add .
git commit -m "Описание изменений"
git push

# Railway автоматически перезапустит проект
```

## 🔧 Решение проблем

### Проблема: "invalid ELF header"
**Решение:** Скрипт `prestart` автоматически пересобирает `better-sqlite3`. Просто перезапустите деплой.

### Проблема: Ошибка 409 "Conflict"
**Решение:** Убедитесь, что бот не запущен локально. Остановите локальный процесс:
```bash
ps aux | grep "node bot.js"
kill <PID>
```

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте `BOT_TOKEN` в Variables
2. Проверьте логи Railway
3. Убедитесь, что `WEB_APP_URL` правильный

### Проблема: Web App не открывается
**Решение:**
1. Проверьте `WEB_APP_URL` в Variables
2. Убедитесь, что URL начинается с `https://`
3. Проверьте логи Railway на ошибки

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи Railway
2. Убедитесь, что все переменные окружения установлены
3. Проверьте документацию Railway: https://docs.railway.app

---

**Готово!** Ваш бот должен работать на Railway! 🎉

