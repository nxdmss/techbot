# 🔧 Исправление ошибки ngrok в приложении

## Проблема
В приложении показывается ошибка:
```
ERR_NGROK_3200
The endpoint unimmaculately-unogled-savanna.ngrok-free.dev is offline.
```

## Причина
Бот использует старый ngrok URL в кнопке Web App. Это происходит потому, что:
1. В переменных окружения все еще указан старый ngrok URL
2. Или бот был запущен со старым URL и не перезапущен

## Решение

### Вариант 1: На Railway (рекомендуется)

1. **Откройте Railway Dashboard**
2. **Откройте ваш проект → Variables**
3. **Удалите или обновите `WEB_APP_URL`:**
   - Если там указан ngrok URL, **удалите** эту переменную
   - Railway автоматически определит URL через `RAILWAY_PUBLIC_DOMAIN`
4. **Убедитесь, что домен сгенерирован:**
   - Settings → Networking → Generate Domain
5. **Перезапустите сервис:**
   - Deployments → Redeploy

### Вариант 2: Локально

1. **Откройте файл `.env`**
2. **Удалите или обновите `WEB_APP_URL`:**
   ```env
   # Удалите эту строку или замените на правильный URL
   # WEB_APP_URL=https://unimmaculately-unogled-savanna.ngrok-free.dev
   ```
3. **Для локальной разработки используйте:**
   ```env
   WEB_APP_URL=http://localhost:3000
   ```
4. **Перезапустите бота:**
   ```bash
   npm start
   ```

### Вариант 3: Обновить URL в боте

Если бот уже запущен со старым URL:

1. **Остановите бота** (Ctrl+C)
2. **Обновите переменные окружения** (см. выше)
3. **Запустите бота заново:**
   ```bash
   npm start
   ```

## Проверка

После исправления:

1. **Проверьте логи:**
   - Должно быть: `📱 Web App URL: https://ваш-railway-url.railway.app`
   - Или: `📱 Web App URL: http://localhost:3000` (локально)

2. **Протестируйте бота:**
   - Отправьте `/start` боту
   - Нажмите на кнопку "🛍️ Открыть магазин"
   - Должно открыться приложение без ошибки ngrok

## Важно

- **На Railway:** Не нужно указывать `WEB_APP_URL` - Railway определяет автоматически
- **Локально:** Используйте `http://localhost:3000` для разработки
- **Ngrok больше не нужен** - используйте Railway для production

