# Telegram Shop Bot (Python Version)

Профессиональный шаблон Telegram магазина на базе Python (FastAPI + Aiogram + SQLAlchemy).

## Функционал

- 🛍️ **Магазин**: Web App (Mini App) с каталогом, корзиной и поиском.
- 🤖 **Бот**: Уведомления о заказах, админ-панель.
- 👨‍💼 **Админка**: Просмотр заказов, статистика, управление статусами заказов прямо в боте.
- 📦 **База данных**: SQLite (легко переключается на PostgreSQL).

## Структура проекта

- `app/`: Основной код приложения
  - `api/`: API эндпоинты (FastAPI)
  - `bot/`: Логика бота (Aiogram)
  - `core/`: Конфигурация
  - `db/`: Работа с БД (SQLAlchemy)
  - `services/`: Бизнес-логика
- `public/`: Frontend (HTML/CSS/JS) для Web App
- `main.py`: Точка входа

## Установка и запуск

1. **Настройка окружения**:
   Скопируйте `.env.example` в `.env` и укажите свой токен бота:
   ```bash
   cp env.example .env
   # Редактируйте .env
   ```

2. **Установка зависимостей**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Запуск**:
   ```bash
   python main.py
   ```

   Бот и Web-сервер запустятся одновременно.

## Развертывание

Проект готов к развертыванию через Docker или на платформах типа Railway.

```bash
docker build -t telegram-shop .
docker run -p 8000:8000 --env-file .env telegram-shop
```
