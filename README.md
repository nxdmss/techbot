# Telegram Web App Bot

Telegram бот с веб-приложением для интернет-магазина.

## 🚀 Быстрый старт на Railway

### 1. Подготовка

1. Убедитесь, что у вас есть:
   - Аккаунт на [Railway](https://railway.app)
   - Telegram бот (создайте через [@BotFather](https://t.me/BotFather))
   - Ваш Telegram ID (узнайте у [@userinfobot](https://t.me/userinfobot))

### 2. Деплой на Railway

#### Вариант A: Через GitHub (рекомендуется)

1. **Закоммитьте проект в GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <ваш-github-repo-url>
   git push -u origin main
   ```

2. **Подключите к Railway:**
   - Откройте [Railway Dashboard](https://railway.app/dashboard)
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   - Откройте проект → Settings → Variables
   - Добавьте следующие переменные:
     ```
     BOT_TOKEN=ваш_токен_бота
     ADMIN_ID=ваш_telegram_id
     NODE_ENV=production
     ```

4. **Настройте домен:**
   - Откройте проект → Settings → Networking
   - Нажмите "Generate Domain" или используйте свой
   - Скопируйте URL (например: `https://your-app.railway.app`)

5. **Обновите WEB_APP_URL:**
   - Вернитесь в Variables
   - Добавьте: `WEB_APP_URL=https://your-app.railway.app` (используйте ваш URL)

6. **Дождитесь деплоя:**
   - Railway автоматически соберет и запустит проект
   - Проверьте логи в разделе "Deployments"

#### Вариант B: Через Railway CLI

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите в аккаунт
railway login

# Инициализируйте проект
railway init

# Установите переменные окружения
railway variables set BOT_TOKEN=ваш_токен
railway variables set ADMIN_ID=ваш_id
railway variables set NODE_ENV=production

# Деплой
railway up
```

### 3. Проверка работы

1. **Проверьте логи Railway:**
   - Должно быть: `✅ Бот успешно подключен: @username`
   - Должно быть: `✅ Polling запущен успешно`
   - Должно быть: `🚀 Бот запущен!`

2. **Протестируйте бота:**
   - Откройте вашего бота в Telegram
   - Отправьте `/start`
   - Должна появиться кнопка "🛍️ Открыть магазин"

3. **Проверьте Web App:**
   - Нажмите на кнопку "🛍️ Открыть магазин"
   - Должно открыться веб-приложение

## 📋 Переменные окружения

| Переменная | Описание | Обязательная |
|-----------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота от @BotFather | ✅ Да |
| `ADMIN_ID` | Ваш Telegram ID (для получения заказов) | ✅ Да |
| `WEB_APP_URL` | URL вашего приложения на Railway | ✅ Да |
| `PORT` | Порт (Railway устанавливает автоматически) | ❌ Нет |
| `NODE_ENV` | Окружение (production/development) | ❌ Нет |

## 🔧 Локальная разработка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <ваш-repo-url>
   cd techbot
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте `.env` файл:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env и добавьте свои значения
   ```

4. **Запустите проект:**
   ```bash
   npm run dev
   ```

## 📁 Структура проекта

```
techbot/
├── bot.js              # Основной файл бота и сервера
├── database.js         # Работа с базой данных
├── security-utils.js   # Утилиты безопасности
├── public/             # Веб-приложение
│   ├── index.html
│   ├── app.js
│   ├── styles.css
│   ├── products.json   # Товары
│   └── images/         # Изображения товаров
├── scripts/            # Вспомогательные скрипты
├── package.json
└── .env.example        # Пример переменных окружения
```

## 🛠️ Команды

- `npm start` - Запуск в production режиме
- `npm run dev` - Запуск в development режиме (с nodemon)
- `npm run rebuild` - Пересборка better-sqlite3

## 🐛 Решение проблем

### Ошибка "invalid ELF header"
- **Причина:** better-sqlite3 скомпилирован для другой платформы
- **Решение:** Скрипт `prestart` автоматически пересобирает модуль

### Ошибка 409 "Conflict: terminated by other getUpdates"
- **Причина:** Запущено несколько экземпляров бота
- **Решение:** Убедитесь, что бот не запущен локально или на другом сервере

### Бот не отвечает
- Проверьте `BOT_TOKEN` в переменных окружения
- Проверьте логи Railway на наличие ошибок
- Убедитесь, что `WEB_APP_URL` правильный

## 📚 Дополнительная информация

- [Railway Documentation](https://docs.railway.app)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Web Apps](https://core.telegram.org/bots/webapps)

## 📝 Лицензия

MIT

