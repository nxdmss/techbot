# Telegram Shop Template (Python) 🐍

Готовый шаблон интернет-магазина в Telegram на Python (Aiogram 3 + FastAPI).
Быстрый, асинхронный, с веб-интерфейсом.

## 🚀 Быстрый старт

1.  **Установка зависимостей:**
    ```bash
    pip install -r requirements.txt
    ```

2.  **Настройка:**
    Создайте файл `.env` (скопируйте из `env.example`) и укажите данные:
    - `BOT_TOKEN`: Токен от @BotFather
    - `ADMIN_ID`: Ваш Telegram ID

3.  **Запуск:**
    ```bash
    python main.py
    ```
    Или через uvicorn:
    ```bash
    uvicorn main:app --reload
    ```

## 📦 Добавление товаров

Редактируйте файл `public/products.json`.

## 🛠️ Технологии

- **Backend**: Python 3.11+, FastAPI, Aiogram 3.x
- **Frontend**: HTML/JS (Telegram Web App)
- **Database**: SQLite (aiosqlite)
- **Deploy**: Docker, Railway

## ☁️ Деплой

Проект готов к деплою на Railway.
Просто подключите репозиторий, и Railway использует `Dockerfile`.
Не забудьте указать переменные окружения.
