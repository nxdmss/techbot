# 📦 Руководство по добавлению товаров

## Простое добавление товаров в `products.json`

Вы можете добавлять товары в `public/products.json` - система автоматически обработает их!

## Минимальный формат товара

Для добавления товара достаточно указать только **название** и **цену**:

```json
{
    "name": "Название товара",
    "price": 1000
}
```

Все остальные поля будут автоматически заполнены дефолтными значениями.

## Полный формат товара

```json
{
    "id": 5,                              // Опционально - автоматически сгенерируется если не указан
    "name": "Название товара",            // Обязательно
    "brand": "Название бренда",          // Опционально - по умолчанию "Без бренда"
    "description": "Краткое описание",   // Опционально - по умолчанию берется из name
    "price": 1000,                        // Обязательно
    "emoji": "🛍️",                       // Опционально - по умолчанию "🛍️"
    "image": "/images/product.jpg",      // Опционально - основное изображение
    "images": [                           // Опционально - дополнительные изображения
        "/images/product1.jpg",
        "/images/product2.jpg"
    ],
    "fullDescription": "Полное описание товара",  // Опционально
    "specs": [                            // Опционально - характеристики
        "Характеристика 1",
        "Характеристика 2"
    ],
    "dateAdded": "2024-11-15T10:00:00Z"  // Опционально - автоматически текущая дата
}
```

## Примеры

### Минимальный товар
```json
{
    "name": "Футболка",
    "price": 1990
}
```

### Товар с изображением
```json
{
    "name": "Куртка",
    "brand": "Nike",
    "price": 5990,
    "image": "/images/jacket.jpg",
    "description": "Теплая куртка"
}
```

### Товар с несколькими изображениями
```json
{
    "name": "Кроссовки",
    "brand": "Adidas",
    "price": 8990,
    "image": "/images/shoes1.jpg",
    "images": [
        "/images/shoes2.jpg",
        "/images/shoes3.jpg"
    ],
    "fullDescription": "Удобные кроссовки для бега",
    "specs": [
        "Материал: кожа",
        "Размер: 42-46",
        "Цвет: черный"
    ]
}
```

## Автоматическая обработка

Система автоматически:
- ✅ Генерирует `id` если не указан (начиная с 1)
- ✅ Добавляет дефолтные значения для всех опциональных полей
- ✅ Объединяет `image` и `images` в один массив
- ✅ Генерирует `dateAdded` если не указан
- ✅ Ограничивает длину текстовых полей для безопасности
- ✅ Валидирует типы данных

## Важные замечания

1. **ID товара**: Если не указан, будет автоматически сгенерирован. Рекомендуется указывать вручную для контроля.

2. **Изображения**: 
   - `image` - основное изображение (будет первым в галерее)
   - `images` - дополнительные изображения
   - Если указано только `image`, оно автоматически добавится в массив `images`

3. **Бренды**: Автоматически создаются фильтры для всех уникальных брендов

4. **Кеширование**: После изменения `products.json` может потребоваться очистка кеша через `/api/products/clear-cache` или перезагрузка страницы с `?refresh=true`

## После добавления товара

1. Сохраните `products.json`
2. Перезагрузите страницу в браузере (или используйте `?refresh=true` в URL)
3. Товар появится в каталоге автоматически!

## Структура файла

Файл `products.json` должен быть массивом объектов:

```json
[
    {
        "name": "Товар 1",
        "price": 1000
    },
    {
        "name": "Товар 2",
        "price": 2000
    }
]
```

